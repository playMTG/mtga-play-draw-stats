# 审查与修复记录

首次审查发现重复回放清空子表、筛选未传入评价、未知调度计为零、缺少基线却输出正常、打标参数不一致和日志轮换漏读等问题。本轮已修复并添加回归测试。

主将历史缺失来自扁平导入流程丢失原始 deckstring 元数据，不能据此声称 Untapped 没有主将资料。补全工具按既有对局身份匹配，执行前备份，保留原有总场数和胜负。个人核对数字保存在忽略的 data/reviews/ 中。

当前口径及已知边界见 [设计文档](DESIGN.md)。
